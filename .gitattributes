# Normalize line endings: auto-detect text files, store with LF in repo
* text=auto

# Shell scripts must always use LF
*.sh text eol=lf

# WinGet manifest templates require CRLF in the working tree
template/Freelensapp* text eol=crlf

# Generated WinGet manifests also require CRLF
manifests/**/Freelensapp* text eol=crlf
